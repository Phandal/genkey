# genkey
Create pgp keys for CP Benefits Connector Clients. This script will not add the pgp key to your
keyring. It will instead use a temporary keyring that gets created in the current directory. The
script will also export the keys automatically as text files.

# Usage
```bash
./genkey
# Follow on screen prompts
```
